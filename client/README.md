# Creator Workspace — client

This is the React frontend. See the [root README](../README.md) for full
setup instructions (backend + MongoDB + this app).

Quick start (backend must already be running on port 5000):

```bash
npm install
npm run dev
```
